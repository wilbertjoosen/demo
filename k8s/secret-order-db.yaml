# Dev-only credentials matching docker-compose.yml's mysql service — never reuse in a real deployment.
apiVersion: v1
kind: Secret
metadata:
  name: order-service-db
  namespace: demo-qa
type: Opaque
stringData:
  DB_USERNAME: demo
  DB_PASSWORD: demo
  ORDER_SERVICE_CLIENT_SECRET: order-service-secret
