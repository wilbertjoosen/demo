apiVersion: apps/v1
kind: Deployment
metadata:
  name: shipping-service
  namespace: demo
  labels: { app: shipping-service, monitored: "true" }
spec:
  replicas: 1
  selector: { matchLabels: { app: shipping-service } }
  template:
    metadata: { labels: { app: shipping-service, monitored: "true" } }
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: shipping-service
          image: ghcr.io/wilbertjoosen/demo-shipping-service:1.0.1-9dd2a67
          imagePullPolicy: Always
          ports: [{ containerPort: 8088 }]
          envFrom:
            - configMapRef: { name: demo-common-config }
            - secretRef: { name: pyroscope-agent }
          env:
            - { name: EUREKA_INSTANCE_HOSTNAME, value: shipping-service }
            - { name: MONGO_DB, value: shipments }
          resources:
            requests: { cpu: "200m", memory: "384Mi" }
            limits: { cpu: "750m", memory: "640Mi" }
          # Gates livenessProbe until the app has finished starting, so a slow JVM start
          # under CPU contention (e.g. a cluster-wide image bump restarting every pod at
          # once) can't trip liveness and cascade the whole namespace into CrashLoopBackOff.
          # 30 x 10s = 5 min startup budget; liveness/readiness take over once it passes.
          startupProbe:
            httpGet: { path: /actuator/health/readiness, port: 8088 }
            failureThreshold: 30
            periodSeconds: 10
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 8088 }
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 8088 }
            timeoutSeconds: 5
            initialDelaySeconds: 30
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: shipping-service
  namespace: demo
  labels: { monitored: "true" }
spec:
  selector: { app: shipping-service }
  ports: [{ name: http, port: 8088, targetPort: 8088 }]
