apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-comment-service
  namespace: demo
  labels: { app: product-comment-service, monitored: "true" }
spec:
  replicas: 1
  selector: { matchLabels: { app: product-comment-service } }
  template:
    metadata: { labels: { app: product-comment-service, monitored: "true" } }
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: product-comment-service
          image: ghcr.io/wilbertjoosen/demo-product-comment-service:1.0.1-604f8c5
          imagePullPolicy: Always
          ports: [{ containerPort: 8091 }]
          envFrom:
            - configMapRef: { name: demo-common-config }
            - secretRef: { name: pyroscope-agent }
          env:
            - { name: EUREKA_INSTANCE_HOSTNAME, value: product-comment-service }
            - { name: MONGO_DB, value: comments }
          resources:
            requests: { cpu: "200m", memory: "384Mi" }
            limits: { cpu: "750m", memory: "640Mi" }
          # Gates livenessProbe until the app has finished starting, so a slow JVM start
          # under CPU contention (e.g. a cluster-wide image bump restarting every pod at
          # once) can't trip liveness and cascade the whole namespace into CrashLoopBackOff.
          # 30 x 10s = 5 min startup budget; liveness/readiness take over once it passes.
          startupProbe:
            httpGet: { path: /actuator/health/readiness, port: 8091 }
            failureThreshold: 30
            periodSeconds: 10
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 8091 }
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 8091 }
            timeoutSeconds: 5
            initialDelaySeconds: 30
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: product-comment-service
  namespace: demo
  labels: { monitored: "true" }
spec:
  selector: { app: product-comment-service }
  ports: [{ name: http, port: 8091, targetPort: 8091 }]
