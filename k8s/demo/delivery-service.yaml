apiVersion: apps/v1
kind: Deployment
metadata:
  name: delivery-service
  namespace: demo
  labels: { app: delivery-service, monitored: "true" }
spec:
  replicas: 1
  selector: { matchLabels: { app: delivery-service } }
  template:
    metadata: { labels: { app: delivery-service, monitored: "true" } }
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: delivery-service
          image: ghcr.io/wilbertjoosen/demo-delivery-service:1.0.1-966aac3
          imagePullPolicy: Always
          ports: [{ containerPort: 8087 }]
          envFrom:
            - configMapRef: { name: demo-common-config }
            - secretRef: { name: pyroscope-agent }
          env:
            - { name: EUREKA_INSTANCE_HOSTNAME, value: delivery-service }
            - { name: MONGO_DB, value: deliveries }
          resources:
            requests: { cpu: "200m", memory: "384Mi" }
            limits: { cpu: "750m", memory: "640Mi" }
          # Gates livenessProbe until the app has finished starting, so a slow JVM start
          # under CPU contention (e.g. a cluster-wide image bump restarting every pod at
          # once) can't trip liveness and cascade the whole namespace into CrashLoopBackOff.
          # 30 x 10s = 5 min startup budget; liveness/readiness take over once it passes.
          startupProbe:
            httpGet: { path: /actuator/health/readiness, port: 8087 }
            failureThreshold: 30
            periodSeconds: 10
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 8087 }
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 8087 }
            timeoutSeconds: 5
            initialDelaySeconds: 30
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: delivery-service
  namespace: demo
  labels: { monitored: "true" }
spec:
  selector: { app: delivery-service }
  ports: [{ name: http, port: 8087, targetPort: 8087 }]
