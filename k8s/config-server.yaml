apiVersion: apps/v1
kind: Deployment
metadata:
  name: config-server
  namespace: demo
  labels: { app: config-server, monitored: "true" }
spec:
  replicas: 1
  selector: { matchLabels: { app: config-server } }
  template:
    metadata: { labels: { app: config-server, monitored: "true" } }
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: config-server
          image: ghcr.io/wilbertjoosen/demo-config-server:ddac784a15a94f04e285bbf762d3385491327ff7
          imagePullPolicy: Always
          ports: [{ containerPort: 8888 }]
          resources:
            requests: { cpu: "100m", memory: "256Mi" }
            limits: { cpu: "500m", memory: "384Mi" }
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 8888 }
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 8888 }
            initialDelaySeconds: 30
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: config-server
  namespace: demo
  labels: { monitored: "true" }
spec:
  selector: { app: config-server }
  ports: [{ name: http, port: 8888, targetPort: 8888 }]
