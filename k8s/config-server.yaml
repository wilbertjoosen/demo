apiVersion: apps/v1
kind: Deployment
metadata:
  name: config-server
  namespace: demo-qa
  labels: { app: config-server, monitored: "true" }
spec:
  replicas: 1
  selector: { matchLabels: { app: config-server } }
  template:
    metadata: { labels: { app: config-server, monitored: "true" } }
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: config-server
          image: ghcr.io/wilbertjoosen/demo-config-server:12d4474669dcfcfe91f819879bbf25fb7f8f9292
          imagePullPolicy: Always
          ports: [{ containerPort: 8888 }]
          resources:
            requests: { cpu: "100m", memory: "256Mi" }
            limits: { cpu: "500m", memory: "384Mi" }
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 8888 }
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 8888 }
            initialDelaySeconds: 30
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: config-server
  namespace: demo-qa
  labels: { monitored: "true" }
spec:
  selector: { app: config-server }
  ports: [{ name: http, port: 8888, targetPort: 8888 }]
