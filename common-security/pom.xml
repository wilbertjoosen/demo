<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.example</groupId>
        <artifactId>demo</artifactId>
        <version>0.0.1-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>
    <artifactId>common-security</artifactId>
    <name>common-security</name>
    <description>Shared JWT resource-server security config for all backend services</description>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
        </dependency>
        <dependency>
            <!-- Every service that pulls in common-security is WebMVC-based, so this is safe to put
                 here once rather than in each of the 9 services' own poms — springdoc auto-configures
                 itself (unlike ResourceServerSecurityConfig, no manual @Import needed). -->
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>3.1.0</version>
        </dependency>
        <dependency>
            <!-- The autoconfiguration that actually wires up a Tracer/OTLP span exporter in Boot 4.x
                 lives specifically in this artifact — micrometer-tracing-bridge-otel +
                 opentelemetry-exporter-otlp alone register no autoconfiguration (verified: neither
                 jar's META-INF/spring/...AutoConfiguration.imports lists anything OTLP-related).
                 One place to add it for every service behind common-security, same as
                 gateway-service getting it directly since it doesn't depend on this module. -->
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-micrometer-tracing-opentelemetry</artifactId>
        </dependency>
    </dependencies>

</project>
